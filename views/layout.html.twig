<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block page_title %}Filer{% endblock %}</title>
    <link rel="stylesheet" href="./assets/style.css">
    <script src="./assets/script.js"></script>
</head>
<body>
    <header>Filer</header>
    <nav>
        {% if username %}
            <a href="?action=home" class="nav">{{ username }}</a><a href="?action=logout" class="nav">Logout</a>
        {% else %}
            <a href="?action=register" class="register nav">Register</a><a href="?action=login" class="login nav">Login</a>
        {% endif %}
    </nav>
{% block content %}{% endblock %}
</body>
</html>
